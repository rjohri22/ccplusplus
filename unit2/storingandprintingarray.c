#include <stdio.h>

int main()
{
    int numbers[5];
 
    // Store values in the array.
    numbers[0] = 10;
    numbers[1] = 20;
    numbers[2] = 30;
    // numbers[3] = 40;
    // numbers[4] = 50;

    // Print each array element.
    printf("%d\n", numbers[0]);
    printf("%d\n", numbers[1]);
    printf("%d\n", numbers[2]);
    printf("%d\n", numbers[3]);
    printf("%d\n", numbers[4]);

    return 0;
}