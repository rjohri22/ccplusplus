#include <iostream>
using namespace std;

int main()
{
    // Declare a variable to store the student's name
    string name;

    // Take input from the keyboard using cin
    cin >> name;

    // Display the entered name on the screen using cout
    cout << "Hello, " << name;

    // Return 0 to indicate successful program execution
    return 0;
}