#include <stdio.h>

int main()                         // main is an identifier (function name)
{
    int age = 25;                  // age is an identifier (variable name)
    int student_marks = 85;        // student_marks is an identifier (variable name)

    printf("Age = %d\n", age);     // age is an identifier
    printf("Marks = %d\n", student_marks); // student_marks is an identifier

    return 0;
}