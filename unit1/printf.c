#include <stdio.h>

int main()
{
    // Declare and initialize variables.
    int age = 25;
    float marks = 85.5;

    // %d displays the integer value.
    printf("Age = %d\n", age);

    // %f displays the floating-point value.
    printf("Marks = %.2f\n", marks);

    return 0;
}