#include <stdio.h>

int main()
{
    int a = 10;
    int b = 20;

    // Calculate the sum of two numbers
    int sum = a + b;

    /*
       Display the result
       on the screen
    */
    printf("Sum = %d", sum);

    return 0;
}