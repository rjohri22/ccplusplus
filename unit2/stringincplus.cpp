#include <iostream>
#include <string>
using namespace std;

int main()
{
    // Declare and initialize a C++ string
    string name = "Hello";

    // Display the string
    cout << "Name: " << name << endl;

    // Display the length of the string
    cout << "Length: " << name.length();

    return 0;
}