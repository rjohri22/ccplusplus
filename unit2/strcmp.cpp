#include <iostream>
#include <string>
using namespace std;

int main()
{
    string first = "apple";
    string second = "apple";

    // Compare two strings
    if (first == second)
    {
        cout << "Strings are equal";
    }
    else
    {
        cout << "Strings are different";
    }

    return 0;
}