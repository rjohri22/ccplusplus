#include <stdio.h>

int main()
{
    int age = 20;

    // Check whether age is 18 or more
    if (age >= 18)
    {
        // This statement executes because the condition is true
        printf("You are eligible to vote.\n");
    }

    return 0;
}